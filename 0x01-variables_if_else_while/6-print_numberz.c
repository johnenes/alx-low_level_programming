#include<stdio.h>
/**
 * main - program that prints the lowercase alphabet in reverse
 * Return: 0 (Success)
 */
int main(void)
{
int num = 0;

while (num <= 9)
{
putchar('0' + num);
num++;
}
return (0);
}
