#include <stdio.h>
/**
 * main - main block
 * description: A dream doesn't become reality through magic
 * Return: Always 0
 */
int main(void)
{
int a[] = {15, 29, 37, 98, 46};
int *p = a + 2;
printf("a[2] = %d\n", *p);  /* Add this line */
return (0);
}
